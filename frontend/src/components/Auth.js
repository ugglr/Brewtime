import React from "react";
import styled from "styled-components";

const Container = styled.div``;

const Auth = () => {
  return (
    <Container>
      <h1>Auth</h1>
    </Container>
  );
};

export default Auth;
